'use client';

/**
 * Create multistream — Placeholder for contributors.
 *
 * TODO: Implement with:
 *   - Asset selector
 *   - Total amount + duration inputs
 *   - Recipients list with weights
 *   - Add/remove recipient rows
 *   - Weight distribution preview
 *   - Submit → builds XDR → Freighter signs
 *
 * Components to use:
 *   - MultiStreamForm (to be created in components/features/multistreams/)
 */
export default function CreateMultistreamPage() {
  return (
    <div>
      <h1 className="mb-6 text-2xl font-bold text-white">
        Create Multistream
      </h1>
      <div className="mx-auto max-w-lg rounded-xl border border-stellar-border bg-stellar-card p-8">
        <p className="text-center text-gray-400">
          Multistream creation form coming soon.
        </p>
      </div>
    </div>
  );
}
